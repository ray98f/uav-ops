<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.uav.ops.mapper.FileMapper">

    <insert id="insertFile">
        insert into sys_file(id, file_name, biz_code, file_url, create_by)
        value (#{id}, #{name}, #{bizCode}, #{url}, #{doName})
    </insert>

    <select id="selectFileInfo" resultType="com.uav.ops.entity.File">
        select id, file_name, biz_code, file_url, create_by from sys_file
        where id in (
        <foreach collection="list" index="index" item="id" separator=",">
            #{id}
        </foreach>
        ) and is_delete=0
    </select>
</mapper>